---
'@platforma-open/milaboratories.immune-assay-data.kind': patch
---

Bump the kind version alongside the widened column-id check. A kind version's content is immutable in the registry: republishing the same version with a different `sourceHash` hard-fails and takes the whole block publish with it.
